version: "3.9"

services:

  downloader:
    build: .
    container_name: download_service

    env_file:
      - .env

    ports:
      - "8080:8080"

    volumes:
      - ./data:/data
      - ./stats:/app